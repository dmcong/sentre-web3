import { helloSentreWeb3 } from "./../src/index";

describe("balancer-amm", () => {
  before("Is generate data!", async () => {
    // ToDo:
  });

  it("Is initialized pool!", async () => {
    helloSentreWeb3();
  });
});
